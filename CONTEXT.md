# AskMetric

AskMetric 是一个 chat-first 商业智能产品，用于将业务问题转化为可审计分析，并确保所有有实际影响的操作都处于人工控制之下。

## 领域语言

**业务用户（Business User）**：
无需编写 SQL，通过对话调查业务表现的人。
_避免使用_：最终用户、非技术用户

**数据分析师（Data Analyst）**：
对业务用户可用的指标定义和分析数据负责的人。
_避免使用_：操作员、数据所有者

**工作区管理员（Workspace Administrator）**：
对工作区成员、数据访问和操作策略负责的人。
_避免使用_：Admin、平台用户

**工作区（Workspace）**：
一组用户的分析数据、指标定义、对话、记忆和审计历史的隔离边界。
_避免使用_：Organization、Tenant、Account

**工作区成员关系（Workspace Membership）**：
为一个用户授予特定工作区内一组权限的关系。
_避免使用_：Keycloak Role、账户类型

**工作区策略（Workspace Policy）**：
应用于工作区内操作的版本化治理规则，包括操作提案是否必须由另一人审批。
_避免使用_：Prompt 指令、Feature Flag

**指标定义（Metric Definition）**：
一个工作区内某项可度量数值的统一业务含义和计算规则。
_避免使用_：KPI 配置、公式

**语义目录（Semantic Catalog）**：
由工作区治理、包含指标定义和分析数据关系的版本化集合。
_避免使用_：Schema Dump、Prompt 上下文

**数据连接（Data Connection）**：
由工作区治理、通往分析数据源的只读访问路径。
_避免使用_：数据库凭据、数据源配置

**数据分级（Data Classification）**：
分配给分析字段的工作区策略，用于决定字段值能否以及以何种形式被查询、返回给 Agent 或写入报告。
_避免使用_：数据库授权、Prompt 指令

**对话（Conversation）**：
工作区内由消息组成的用户可见线程，可以不包含分析任务，也可以包含多个分析任务。
_避免使用_：分析任务、Chat Session

**消息（Message）**：
对话中的一条用户、助手或系统通信。
_避免使用_：Prompt、Agent Run

**对话摘要（Conversation Summary）**：
对一段明确消息范围的版本化派生摘要，用于减少上下文体积，但不替代原始消息。
_避免使用_：对话、用户记忆

**Agent 运行（Agent Run）**：
由一条用户消息触发的一次执行，可以只产生对话回复，也可以推进一个分析任务。
_避免使用_：消息、分析任务

**意图路由（Intent Route）**：
对用户消息的已记录解释，标明其属于对话、分析、任务控制还是审批。
_避免使用_：工具决策、隐藏分类

**上下文包（Context Pack）**：
提供给一次 Agent 运行的、有边界且可追溯的任务状态、相关对话历史、受治理知识和工具证据集合。
_避免使用_：完整聊天历史、Prompt

**用户记忆（User Memory）**：
经用户确认、可在同一工作区的多个对话间保留的个人偏好。
_避免使用_：对话摘要、指标定义、推断事实

**分析任务（Analysis Task）**：
具有分析目标、生命周期和可验证结果的持久工作单元，可以跨越多条消息和多次 Agent 运行。
_避免使用_：消息、Agent 运行、工具调用

**分析 Agent（Analysis Agent）**：
通过受治理的分析工具和显式工作流状态推进分析任务的唯一有状态运行角色。
_避免使用_：SQL Agent、RAG Agent、Report Agent

**可审计分析（Auditable Analysis）**：
指标定义、证据、推理路径和不确定性均可检查的结构化分析结果。
_避免使用_：答案、响应、洞察

**分析报告（Analysis Report）**：
可查看、下载且无需再次运行 Agent 即可重现的独立、不可变 HTML 可审计分析呈现。
_避免使用_：可审计分析、聊天响应、原始 HTML

**证据快照（Evidence Snapshot）**：
可审计分析实际使用的、受策略限制的查询结果与来源信息，独立于数据源后续变化而保留。
_避免使用_：源表副本、实时查询结果

**参考场景（Reference Scenario）**：
用于展示和评测 AskMetric 通用能力、但不限定产品行业边界的具体端到端分析任务。
_避免使用_：垂直领域、硬编码 Demo

**评测用例（Evaluation Case）**：
带有明确证据和结果标准、用于评估分析任务的可重复业务问题。
_避免使用_：测试 Prompt、Benchmark Query

**评测集（Evaluation Suite）**：
用于持续比较 AskMetric 行为的版本化评测用例及其结果集合。
_避免使用_：测试套件、Demo 脚本

**分析失败（Analysis Failure）**：
明确指出无法产出可验证结果所处阶段的分析任务结果。
_避免使用_：错误答案、异常

**知识来源（Knowledge Source）**：
工作区治理的业务文档、指标说明或数据字典，可在分析期间作为带引用的证据使用。
_避免使用_：用户记忆、对话历史

**演示工作区（Demo Workspace）**：
包含可重置合成数据和受限能力的公开工作区，用于在不暴露外部系统的前提下评估 AskMetric。
_避免使用_：Sandbox Tenant、生产工作区

**演示数据仓库（Demo Warehouse）**：
供演示工作区使用、包含 B2B SaaS 合成历史的确定性分析数据集。
_避免使用_：应用数据库、Fixture Database

**植入事件（Planted Event）**：
有意加入演示数据仓库、具有已知原因和可测影响、作为评测 Ground Truth 的业务情况。
_避免使用_：随机噪声、脚本化答案

**已批准操作（Approved Action）**：
在分析任务中提出、执行前已经由人工明确授权的外部副作用。
_避免使用_：工具调用、自动操作

**后续任务（Follow-up Task）**：
根据可审计分析提出，并且只能作为已批准操作创建的外部工作项。
_避免使用_：Ticket、Agent Task

**操作提案（Action Proposal）**：
等待人工决定、包含确切参数且不可变的特定外部副作用请求。
_避免使用_：已批准操作、工具调用

**审批（Approval）**：
人工对一个确切操作提案的授权；提案发生任何变化都会使审批失效。
_避免使用_：确认消息、统一授权

**审计记录（Audit Record）**：
关于谁尝试或授权了受治理操作、实际发生了什么以及结果如何的持久证据。
_避免使用_：应用日志、Trace Span
